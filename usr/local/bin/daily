#!/bin/busybox sh

# Exit if an error occurs or an unset variable is referenced.
set -euo pipefail

# As the unprivileged `acme` user, check if our TLS certificates should be
# renewed yet. `dehydrated` renews certificates 30 days before they expire.
# Let's Encrypt certificates expire after 90 days, so this renews them every 60.
su acme -s /bin/sh -c /home/acme/bin/issue
